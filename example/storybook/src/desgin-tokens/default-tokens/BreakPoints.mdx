import { Meta } from '@storybook/blocks';
import {
  DefaultBreakPointsComponent,
  StringBreakPointsComponent,
} from './Component';

<Meta title="Design Tokens/Breakpoints" />

# Breakpoints

Use the breakpoint prefixes <StringBreakPointsComponent /> to apply styles at different viewport sizes.

<DefaultBreakPointsComponent />
